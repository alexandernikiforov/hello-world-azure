language: java

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

jdk:
  - oraclejdk11

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.m2

install:
  - ./gradlew wrapper
  - ./gradlew -v

script:
  - ./gradlew build --continue

after_success:
  - curl -v --ftp-create-dirs --user ${FTP_USER}:${FTP_PASSWORD} -Q "DELE /site/wwwroot/webapps/hello-azure.war" ftp://${FTP_ENDPOINT}/
  - curl -v --ftp-create-dirs --user ${FTP_USER}:${FTP_PASSWORD} -T hello-azure-war/build/libs/hello-azure-1.0.0.war ftp://${FTP_ENDPOINT}/webapps/hello-azure.war